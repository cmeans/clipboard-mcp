Read tabular data from the clipboard. Alias for clipboard_paste — prefer
calling clipboard_paste instead, which handles all content types including
tables. This tool exists for backward compatibility and behaves identically.
